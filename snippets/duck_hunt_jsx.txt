	return (
		<main>
			<h1>🔫 Hunt em, {name || "???"}!</h1>

			<h2>Score: {score}</h2>

			<figure
				style={{
					position: "relative",
					width: width,
					height: height,
					border: "2px solid",
				}}>
				<p
					onClick={shootDuck}
					style={{
						position: "absolute",
						left: duck.x,
						top: duck.y,
						cursor: "crosshair",
					}}>
					{duck.alive ? "🦆" : "💥"}
				</p>
			</figure>
		</main>
	);
